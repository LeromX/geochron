<Window x:Class="GeochronScreensaver.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeochronScreensaver.UI"
        mc:Ignorable="d"
        Title="Geochron"
        Height="720" Width="1280"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="Black">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="#e0e0e0">
            <MenuItem Header="_File">
                <MenuItem Header="Set as _Screensaver" Click="SetAsScreensaver_Click"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click" InputGestureText="Ctrl+Q"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem x:Name="FullscreenMenuItem" Header="_Fullscreen" Click="ToggleFullscreen_Click" InputGestureText="F11"/>
                <Separator/>
                <MenuItem Header="_Save Globe Position" Click="SaveGlobePosition_Click" InputGestureText="Ctrl+S"/>
                <MenuItem Header="_Reset Globe Position" Click="ResetGlobePosition_Click"/>
            </MenuItem>
            <MenuItem Header="_Settings">
                <MenuItem Header="_Configure..." Click="Configure_Click" InputGestureText="Ctrl+,"/>
            </MenuItem>
        </Menu>

        <!-- Main Display Area -->
        <ContentControl x:Name="DisplayContainer" Grid.Row="1"/>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="#1a1a1a" Foreground="#aaaaaa">
            <StatusBarItem>
                <TextBlock x:Name="ModeDisplay" Text="Mode: --"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="ProjectionDisplay" Text="Projection: --"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
